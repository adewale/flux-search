# Comparative Port Plan (archived)

This comparative audit has been folded into Flux-native specs and code.

- Queue architecture, operational reliability, metrics, rollout gates, and
  remaining backlog now live in `specs/flux-queues.spec.md`.
- Topic extraction, topic surfaces, `topic:` search, and related issue behavior
  live in `specs/yaket-topics.spec.md`.

Keep this file only as a pointer so new work uses Flux Search naming and does
not treat Bobbin as the implementation vocabulary.
